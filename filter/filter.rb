require 'json'
require 'termextractor'

class Filter

  # Make extraction list

  # Loop through all files in dir (or build into dir import)
  # Extract terms from data
  # Keep other non-matching jobs for profile
  # Keep file/directory tags (add new ones too on import)
  # Integrate with importer
end

# Get running on rails app mult files
# Get online
# Send out
